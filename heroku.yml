setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    HOST: 0.0.0.0
    JWT_SECRET: myjwtsecret
build:
  docker:
    web: api/Dockerfile.web
release:
  command:
    - ./release_tasks.sh
  image: web
  config:
    DATABASE_URL: ${DATABASE_URL}
