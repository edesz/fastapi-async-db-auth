setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    HOST: 0.0.0.0
    JWT_SECRET: myjwtsecret
build:
  docker:
    web: api/Dockerfile.web
release:
  command:
    - DATABASE_URL=$(heroku config:get DATABASE_URL -a fastapi-minimal-mlc) ./release_tasks.sh
  image: web
